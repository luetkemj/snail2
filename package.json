{
  "name": "snail2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://luetkemj.github.io/snail2",
  "scripts": {
    "start": "parcel src/index.html",
    "devDemon": "nodemon -w src -x 'NODE_ENV=production parcel src/index.html'",
    "build": "parcel build src/index.html --public-url /snail2/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.15",
    "overprint": "^0.8.1",
    "rung": "^0.5.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^2.1.1",
    "nodemon": "^1.19.4"
  }
}
